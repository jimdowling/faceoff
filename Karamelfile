dependencies:
  - recipe: faceoff::install
  - recipe: faceoff::experiment
    local:
      - faceoff::install
    global:
      - spark::worker
      - flink::taskmanager
  - recipe: faceoff::blah
    local:
      - faceoff::install
    global:
      - spark::worker
      - flink::taskmanager


